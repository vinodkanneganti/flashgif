/**
 * Media module: upload orchestration (S3 presigned URLs), metadata,
 * transcoding pipeline (RabbitMQ + FFmpeg worker), and the outbox publisher
 * that the search module consumes.
 */
package com.flashgif.media;
